#pragma once
#include <vector>
#include <string>
#include <algorithm>
#include "game.h"

class BehaviourTree {
public:
	class Node {
		BehaviourTree * boss;
		virtaul bool run(Leg &leg)= 0
			
	}
}
